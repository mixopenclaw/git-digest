Commands:

- opencode dev    # runs the dev profile (hot reload)
- opencode build  # builds the project
- opencode preview# runs the preview profile (production-like)
- opencode sync   # sync local config to opencode workspace

Notes:
- Uses pnpm scripts (pnpm dev, pnpm build, pnpm start). Adjust if your project uses npm or yarn.
