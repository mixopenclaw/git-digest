package main

import "fmt"

// scanCommand is a placeholder for the scan subcommand.
func scanCommand(path, format string) error {
	fmt.Printf("scan: path=%s format=%s\n", path, format)
	return nil
}
